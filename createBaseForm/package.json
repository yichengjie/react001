{
  "name": "webpack-app",
  "version": "1.0.0",
  "description": "webpack app description",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "webpack-dev-server --progress --profile --colors --hot --port 3000",
    "build": "webpack --progress --profile --colors"
  }
}